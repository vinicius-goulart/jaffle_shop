sources:
  - name: stripe
    database: dbt-tutorial
    schema: stripe
    tables:
      - name: payment
        description: Raw payment transactions from Stripe.
        config: 
          loaded_at_field: _batched_at
          freshness: 
            warn_after: 
              count: 12
              period: hour
            error_after: 
              count: 24
              period: hour
